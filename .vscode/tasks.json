{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run main.py",
            "type": "shell",
            "command": "${command:workbench.action.terminal.kill}; & ${workspaceFolder}\\venv\\Scripts\\activate; python ${workspaceFolder}\\main.py",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}